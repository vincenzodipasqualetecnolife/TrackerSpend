name: tracker_spend_backend
description: "Simple Dart backend for TrackerSpend"
version: 1.0.0

environment:
  sdk: ^3.9.0

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  shelf_cors_headers: ^0.1.5
  mysql1: ^0.20.0

dev_dependencies:
  test: ^1.24.0
